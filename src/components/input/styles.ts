import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({

    input:{
        width: "100%",
        height: 50,
        borderRadius: 8,
        borderWidth: 1,
        borderColor: "#000",
        padding: 12,
        fontSize: 18,
    }
})